# Secrets fixture - one file, four detector families.
# All values are FABRICATED (see deploy.sh) but format-valid.
github:
  token: "ghp_CODESECTESTBED0000000000000000000000"                 # GitHub personal access token
slack:
  bot_token: "xoxb-0000000000-0000000000-CODESECTESTBEDexample0"
  webhook: "https://hooks.slack.com/services/T00000000/B00000000/CODESECTESTBEDexample"
stripe:
  secret_key: "sk_live_CODESECTESTBED0000000000"
database:
  # Credential embedded in a connection string - the most common real-world shape.
  url: "postgres://svc_reporting:Pa55w0rd-CODESECTESTBED@db.internal:5432/analytics?sslmode=disable"
jwt:
  signing_secret: "CODESECTESTBED-do-not-use-this-as-a-signing-secret"
ssh:
  # The private key next to this file is a genuine 2048-bit RSA key, freshly
  # generated for this repository and installed nowhere. It exists so the
  # detector has a real PEM block to match.
  key_path: "./id_rsa_testbed"
